
# Installer routes

/installer:
  controller: Installer
  action: index

/installer/process:
  controller: Installer
  action: configDatabase

/installer/account:
  controller: Installer
  action: createAdmin

/installer/validate:
  controller: Security
  action: validate

/installer/login:
  controller: Security
  action: login

/installer/forgot-password:
  controller: Security
  action: forgotPassword

# dashboard routes

/dashboard:
  controller: Dashboard
  action: index

/dashboard/page-builder:
  controller: Dashboard
  action: index

/dashboard/article:
  controller: Dashboard
  action: index

/dashboard/page-builder/delete-page:
  controller: PageBuilder
  action: deletePage

/dashboard/page-builder/create-article:
  controller: Dashboard
  action: index

/dashboard/page-builder/create-page:
  controller: Dashboard
  action: index

/dashboard/page-builder/save-article:
  controller: Article
  action: save

/dashboard/page-builder/create-page/save:
  controller: PageBuilder
  action: savePage

/dashboard/page-builder/update-page:
  controller: PageBuilder
  action: updatePage

/dashboard/template:
  controller: Dashboard
  action: index

/dashboard/comment:
  controller: Dashboard
  action: index

/dashboard/user:
  controller: Dashboard
  action: index

/dashboard/logout:
  controller: Security
  action: logout

/dashboard/update-password:
  controller: Security
  action: changePassword

/dashboard/update-user:
  controller: User
  action: editUser



/user/login:
  controller: Security
  action: login

/user/register:
  controller: Security
  action: register

/user/validate:
  controller: Security
  action: validate

/user/forgot-password:
  controller: Security
  action: forgotPassword

/user/reset-password:
  controller : Security
  action: changePassword

/user/comment:
  controller: Comment
  action: create